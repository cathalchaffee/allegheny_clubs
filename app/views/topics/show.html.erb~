<p id="notice"><%= notice %></p>

<p>
  <strong>Text:</strong>
  <%= @topic.text %>
</p>

<% @comments.reverse.each do |topic| %>
  <%= simple_format(topic.text) %>
<% end %>

<%= link_to 'Edit', edit_topic_path(@topic) %> |
<%= link_to 'Back', topics_path %>
