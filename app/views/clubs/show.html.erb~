
<p id="notice"><%= notice %></p>
<header class="navbar navbar-inverse" id="club-head">
  <div class="container-fluid">
    <h4 id='club-header'>
    <strong>Club:</strong>
    <%= @club.name %>
    </h4>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        <li class="btn-link navbar-btn"><%= link_to 'Edit', edit_club_path(@club) %></li>
      </ul>
    </nav>
  </div>
</header>

<% if logged_in? %>
  <%= render 'users/profile' %>
<% end %>

  <div class="col-md-10">
    <div class="col-md-2"></div>
    <div class="col-md-8 container" id="back-drop">

      <div class="col-md-12" id="form-div">
        <%= render 'topics/form' %>
      </div>
      
      <div class="col-md-12 all-topics">
        <% @topics.reverse.each do |topic| %>
          <div class="topic-div">
          <%= simple_format(topic.text) %>
          <%= link_to 'Comments', topic_path(topic) %>
          <%= link_to 'Delete', topic, method: :delete, data: { confirm: 'Are you sure?' } %>
          </div>
        <% end %>
      </div>
    </div>
    <div class="col-md-2"></div>
  </div>
