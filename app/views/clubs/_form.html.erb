<%= form_for(@new_club) do |f| %>
  <% if @new_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@new_club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
      <% @new_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>

  <div class="field">
    <%= f.label :about %><br>
    <%= f.text_area :about, class: 'form-control' %>
  </div></br>

  <div class="actions">
    <%= f.submit "Create Club", class: "btn btn-primary" %>
  </div>
<% end %>
